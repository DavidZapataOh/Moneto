# @moneto/solana

Solana helpers used across mobile + api. Populated incrementally:

- **Sprint 3** — balance fetching (`BalanceService`), Pyth oracles (`PriceService`), Jupiter swaps (`JupiterSwapService`).
- **Sprint 5** — Umbra SDK wrapper (`PrivacyService`), Token-2022 Confidential Balances (`ConfidentialBalanceService`).
- **Sprint 7** — Squads multisig (`GuardianVaultService`).

Stub for Sprint 0 — only exports a version constant so the package resolves cleanly.
